<h1>Ca...errrrrr Burgers!</h1>

<h2>Ca...errrrrr Burgers that are not devourd!</h2>

<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Ca...errrrrr Burgers that are sleepy!</h2>

<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bur">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devo">devour?</label><br>
    <input type="radio" name="devour" value="1" checked> Sleepy!<br>
    <input type="radio" name="devour" value="0"> Awake
  </div>

  <button type="submit">Add Cat</button>
</form>
